# 中国語学習総合アプリ

## プロジェクト概要
既存のHSK学習系サイト（HSKpractice_exercises, HSKsentences,
HSKsentences_list, HSKspeaking_practice）を1つのアプリに統合する。
単発の静的サイトではなく、ログイン・複数端末同期・AI出題・
共有機能を備えた本格アプリとして再構築する。

## 統合の対象範囲（重要）
- 統合する: HSKpractice_exercises, HSKsentences, HSKsentences_list,
  HSKspeaking_practice（HSK試験対策シリーズ）
- 統合しない: china_travel_phrasebook, mimic_chinese
  これらは独立したツールとして現状のまま維持する（旅行フレーズ集・
  発音まねっこは用途・体験が異なるため、ログイン必須の総合アプリに
  組み込まない方針）

## 目的・ゴール
- ユーザーが自分の学習進捗を1箇所で管理できる
- AIが進捗・弱点に応じて問題を動的に出題する
- 友達と学習内容を共有できる

## 参照ドキュメント一覧
このCLAUDE.mdは「今すぐ必要な情報」だけに絞っています。詳細仕様は
docs/フォルダの別ファイルに分けているので、該当フェーズに入ったら
そのファイルを読み込んでください（毎回全部読むとトークンを消費するため、
必要なタイミングでだけ読む運用にしています）。

- docs/future-features.md: AI学習管理・テスト・テスト結果・辞書・
  発音・話す・シャドーイング・音声(複数話者)等の詳細仕様。
  下記「開発の優先順位」の5番まで終わってから読む
- docs/contract-plan.md: 契約・料金プラン。商用化が近づいたら読む
- supabase-schema-draft.mermaid: ER図（基本テーブルのみ、参考用）
- writing-image-list.md: ライティング機能の画像描写用、必要画像リスト

## 技術スタック
- フロントエンド: Next.js（Supabaseとの公式SSR連携、Vercelとの相性を重視して選定）
- バックエンド: Supabase (PostgreSQL, Auth, Row Level Security)
- AI出題: Claude API（2026-08-23確定: Next.jsのServer Actionから直接呼び出す方式。
  APIキーはサーバー側の環境変数(.env.local / Vercelの環境変数)にのみ保持し、
  クライアントには渡さない。Supabase Edge Functions経由への変更は
  将来必要になった場合に検討する）
- ホスティング: Vercel（開発中はHobby、商用化のタイミングでProへ切り替え）

## データ設計方針（2026-08-20 確定）
以下のテーブル構成で設計する。ER図: supabase-schema-draft.mermaid を参照。

### 基本
- users: ユーザー情報、現在のHSKレベル、配色テーマ(theme列)
- progress: 学習履歴。item_type(word/sentence/passage/pattern等) + item_id の
  組み合わせで、単語・例文・長文などあらゆる種類の学習対象を1テーブルで管理する方式
- shares: 共有機能（誰が誰に何を共有したか。2026-08-23実装済み。
  外部SNSへの投稿ではなく、アプリ内でフォローしている相手に例文・長文を
  共有するだけの内部機能。item_typeはprogressと同じ方針(word/sentence/
  passage/pattern等)で、UIはsentence(`/sentences`)とpassage
  (`/reading/[id]`)の2種類のみ実装。共有一覧は`/shares`）
- follows: フォロー関係（follower_id / following_id）

### 既存4サイト統合分
- words: 単語（HSKpractice_exercises由来）。source_site列で移行元を記録。
  音声は単一のaudio_url列ではなく、下記「音声（複数話者）」のaudio_variants
  テーブルで管理する
- sentences: 例文（HSKsentences由来）。grammar_point_idで文法と紐づけ。
  source_site列あり。音声もwords同様audio_variantsテーブルで管理
- grammar_points: 文法解説（HSKpractice_exercises由来）
- conversations / conversation_lines: 会話練習（HSKspeaking_practice由来）。
  1つの会話(conversations)に複数のセリフ(conversation_lines)が紐づく1対多構成

### 新機能4つ分
- word_groups / word_group_items: グループ暗記（対義語・類義語・数字・色のセット）。
  word_group_itemsのrole列でペアの左右等の位置づけを表現
- long_passages / passage_questions: 長文読解。long_passagesはhsk_level(4/5/6)を持ち、
  1つの長文に設問5問(passage_questions)が紐づく1対多構成
- sentence_patterns: 口語・ビジネス例文パターン集。style列で口語/硬文を区別
- 段階的暗記: 新規テーブルは作らず、progressテーブルの学習履歴を参照して
  「既習単語を使った例文を出す」ロジックをアプリ側で実装する方針

### ヒアリング・ライティング機能分
- listening_questions: ヒアリング問題（選択式/ディクテーション両対応）
- writing_scramble_questions: 語順並べ替え問題
- writing_topics: 自由作文・場面設定ライティングのお題
  （category列で free_topic/scenario を区別）
- writing_image_prompts: 画像描写ライティング用の画像とヒント
- writing_submissions: 自由記述系4種類（自由作文/場面設定/画像描写/長文要約）
  すべての提出内容＋AI添削結果を一元管理する履歴テーブル
  （item_type列で種類を区別、長文要約はlong_passagesのidを参照）

※音声(複数話者)関連テーブル(voices, audio_variants)はdocs/future-features.md参照

## 開発の優先順位
1. 認証 + データ同期の実装（Supabase Auth。メール＋パスワード認証、
   Googleログイン、パスワードリセット機能を含む。詳細は「設定」内の
   「ログイン設定」セクション参照）
2. 既存コンテンツの統合（まず HSKpractice_exercises から着手）
3. AI出題機能（進捗データを見て弱点を突く問題を生成）
4. SNS機能（共有・フォロー等）
5. 追加コンテンツ機能（詳細は下記「追加コンテンツ仕様」を参照）

※AI学習管理・テスト・テスト結果・辞書・発音・話す・シャドーイング等の
詳細は、上記5までが完成した後に着手する規模の大きい機能群。
docs/future-features.mdに仕様をまとめてあるので、実装フェーズに
近づいたタイミングで読み込んで進める（一度に全部設計すると散漫に
なるため、あえて分離している）。

## 追加コンテンツ仕様


### ① グループ暗記モード
意味のまとまりで単語を塊として一気に覚える機能。
「テーマ別」「品詞別」の2つの軸を持ち、どちらからも同じ単語セットに
辿り着けるようにする（同じ単語が両方の軸に重複して登場してよい）。

**テーマ別**
- 対義語セット（例: 押す⇄引く）
- 類義語セット
- 数字（一〜十、百・千・万）
- 色（红色/蓝色/黑色 等）
- 曜日・時間（星期一〜日、今天・明天・昨天）
- 家族（爸爸・妈妈・哥哥・姐姐 等）
- 量詞（个・本・杯・件 等。つまずきやすいため独立枠として重視）

**品詞別**
- 実詞: 名詞、動詞、形容詞、代詞、数詞、量詞
- 虚詞: 介詞、副詞、助詞、接続詞、感嘆詞、擬声語

カテゴリごとにセット化し、グループ単位で表示・練習できるUIが必要。
word_groupsテーブルのgroup_type列で「テーマ別/品詞別のどちらの軸か」と
「具体的な分類（対義語/名詞 等）」の両方を表現する設計にする。

### ② 段階的暗記モード
ユーザーが覚えた単語が、後で出てくる例文に登場する仕組み。
progressテーブル（学習履歴）と連動させ、既習単語を使った例文を
順番に出題する。新出単語は既習語彙の中に少しずつ混ぜていく設計。

### ③ 長文読解（HSK4級・5級・6級対象、2026-08-23実装済み）
- 各レベルごとに、そのレベルの単語・文法のみを使った長文を作成
  （4級長文は4級の単語・文法のみ、5級長文は5級のみ、6級長文は6級のみ。
  レベルを跨いで語彙を混在させない）
- 長文1本につき設問5問。設問文・選択肢とも実際のHSK読解問題と同様に
  中国語のみ（日本語は使わない）
- レベル別に長文セットを分けて管理する
- 本数: 各レベル30本ずつ（HSK4/5/6 合計90本、実装済み）
- テーマの方向性: 全レベル共通で「日常生活で実際に使える内容」を土台にする。
  レベルが上がるにつれて扱うトピックの幅を広げる
  - HSK4: 日常生活の出来事、簡単な旅行記
  - HSK5: 上記に加え、仕事・学校での出来事、ちょっとした意見文
  - HSK6: 上記に加え、社会的な話題、やや抽象的な内容
- テーブル: long_passages（id/hsk_level/title/body）、
  passage_questions（id/passage_id/question_order/question_text/
  choices(jsonb)/correct_choice_index）
- 実装メモ:
  - 字数はCLAUDE.md目安の500字よりやや短め（実際は平均326〜391字、
    範囲294〜482字）。ユーザーレビューの上でこの範囲を許容する方針とした
  - 語彙レベルの逸脱は、既存wordsテーブルを使った貪欲最長一致の分かち書き
    チェックで検知しているが、複合語の分割誤りなどの誤検知を含む簡易チェック
  - 4択の正解位置はA〜Dに均等に分散するよう、コンテンツ作成時点
    （CSV生成スクリプト側）で固定でシャッフル済み。表示のたびに変える
    のではなく、データとして固定した位置を使う方針
  - 90本の作成はHSK4/5/6ごとに並列のサブエージェントで分担して生成した

### ④ 例文パターン集
- 口語パターン: 日常会話で使えるフレーズを100パターン
- 硬文パターン: ビジネスシーンを想定した文を100パターン
- 単語・文法はなるべくHSK5級レベルを使用
- 口語と硬文で明確にトーンを分ける（タメ口〜丁寧な日常会話 / ビジネス敬語調）

**シチュエーション分類（確定）**
- 口語（日常）: 友達との約束、買い物・値段交渉、体調不良・病院、
  レストラン・注文、道を尋ねる、SNS・チャット表現
- ビジネス（硬文）: メールの書き出し・結び、会議での発言、依頼・お願い、
  謝罪・お詫び、自己紹介・名刺交換、電話応対
- 100パターンをこれらのシチュエーションに振り分けて作成する
  （1シチュエーションあたり目安 口語約16〜17パターン、ビジネス約16〜17パターン）

### ⑤ テーマ（配色）切り替え機能
ユーザーが好みに応じてアプリ全体の配色セットを切り替えられる機能。
マスコットキャラクターの配色もテーマに連動して変わる。

- プリセットは5種類（2026-08-23: 北欧カラーセットをグリーン・パープルの2セットに置き換え）:
  1. パステルイエベセット（デフォルト）: ピンクベース。ラベンダー×ピンク基調
  2. パステルブルベセット: ブルーベース。グリーン・パープル、差し色にイエロー
  3. グリーンセット: グリーンベース。ジェイド・パープル、差し色にゴールド
  4. パープルセット: パープルベース。ジェイド・パープル、差し色にゴールド
  5. 地雷カラーセット: 黒に近いダーク×ホットピンク×パープル

- 実装方針: 色をCSS変数（--paper, --ink, --seal, --jade, --gold, --lavender, --line 等）で
  一元管理し、テーマごとに変数セットを差し替える方式にする。マスコットキャラクターの
  イラストも、体・パーツの色をCSS変数参照にしておくことで、テーマ切り替え時に
  自動で連動して配色が変わるようにする（キャラクターごとに個別着色し直す必要がない設計）
- ユーザーが選んだテーマは、ログイン後はSupabaseのusersテーブルにも保存し、
  複数端末で同じテーマが適用されるようにする
- プロトタイプ（動作確認用HTML）: top-page-mockup-character.html内に
  テーマ切り替えボタンとJS実装済み。実装時の配色定義の参考にする

### ⑥ ヒアリング機能（2026-08-23実装、音声方式を暫定変更）
- 選択式: 音声を聞いて、意味の選択肢（誤答含む複数）から正解を選ぶ
- ディクテーション: 音声を聞いて、聞き取った中国語を自分で入力する
  （簡体字入力。判定は完全一致方式で実装）
- 両形式を用意し、ユーザーが選べるようにする
- **音声方式の変更**: 当初想定していたAzure/Google Cloud TTS（audio_variants
  テーブル経由）は未契約のため、ブラウザ標準のWeb Speech API
  （SpeechSynthesis、無料・追加契約不要）でtext_zhを都度読み上げる方式に
  変更した。これに伴いlistening_questionsテーブルはaudio_source（word_id/
  sentence_id参照）ではなく、読み上げ用のtext_zh列を直接持つ自己完結型の
  設計にしている（他の新規コンテンツテーブルと同じ、CSV一括投入を想定した
  構成）。有料TTSへの切り替えは将来の課題として残す（音質・話者数の観点で
  Web Speech APIより優れるため、商用化のタイミングで再検討）
- テーブル: listening_questions
  （hsk_level、mode: choice/dictation、text_zh: 読み上げるテキスト、
  correct_answer、choices: 選択式の場合の選択肢(jsonb)）
- 初期データ: wordsテーブルを元に、選択式・ディクテーションそれぞれ
  HSK1〜6級×10問ずつ、計120問を自動生成（選択式の誤答は同じHSKレベルの
  単語から優先的に選出し、難易度を揃えている）

### ⑦ ライティング機能
日本語を介さず中国語で発想する力を鍛えることを重視する。
そのため翻訳練習（日本語→中国語）は採用しない。

- **語順練習**: シャッフルされた単語を並べ替えて正しい文を作る
  - 新規テーブル: writing_scramble_questions
    （hsk_level、words_shuffled: シャッフル済み単語配列(jsonb)、
    correct_sentence、meaning_ja）

- **自由記述系（4種類、すべてAI添削）**: 提出した文章をClaude API
  （AI出題機能と同じ仕組み）で添削・フィードバックする
  1. **自由作文**: 与えられたテーマについて自由に書く（実装済み）
  2. **場面設定ライティング**: お題自体を中国語のみで提示する
     ロールプレイ的な設定（例: 写一写你周末做了什么）。日本語の指示文を
     使わないことで、日本語を介さず中国語で発想する練習になる（実装済み）
  3. **画像描写**: 画像を見て、その内容を中国語で説明する。
     日本語の文章が一切介在しない、最も直接的な方法（画像素材が未準備のため未実装）
  4. **長文要約**（2026-08-23実装済み）: 長文読解（③）で読んだ文章を、
     自分の言葉で中国語のまま要約する。読解とライティングを連動させる。
     `/reading/[id]/summary` から、その長文の原文を見ながら要約を提出し、
     `submitPassageSummary`（`src/lib/writing/actions.ts`）でAI添削する

  - 新規テーブル: writing_topics
    （hsk_level、category: free_topic/scenario の区別、prompt_text、
    meaning_jaのヒントは最小限 or なし）
  - 新規テーブル: writing_image_prompts（hsk_level、image_url、
    ヒントキーワード）※必要画像リストはwriting-image-list.mdを参照
    （HSKレベル別に合計60枚の画像テーマ一覧。他AIでの画像調達・生成に使用）
  - 長文要約は既存のlong_passagesテーブルを参照し、専用テーブルは作らない
  - 新規テーブル: writing_submissions（user_id、item_type: topic/image/
    passage_summary、item_id、submitted_text、ai_feedback、created_at。
    4種類すべての提出履歴をこの1テーブルで一元管理し、progressとも連動）

## コーディング規約
以下は理想論ではなく、実際にこれまでの実装で守られてきたルールを書き起こしたもの。
既存コードがこの通りになっていない箇所があっても、今回は一括修正しない
（新規実装・改修時にこのルールを適用していく）。

- **フォルダの役割分担**: `src/app/<機能>/`はルーティング専用（page.tsx、必要なクライアント
  コンポーネント）。`src/lib/<機能>/`は実装ロジック本体を置く場所で、役割ごとにファイル名を
  揃える（Server Actionは`actions.ts`、データ取得・出題ロジックは`select.ts`、React Contextは
  `XxxContext.tsx`、静的な定義値は`xxxMeta.ts`）。`src/components/`は複数画面をまたいで使う
  共有UIコンポーネントを置く場所。この対応関係を崩さない
- **命名規則**: Reactコンポーネント本体とそのファイル名はPascalCase（`GoalCard.tsx`等）。
  それ以外のロジックファイルはcamelCase（`reviewProgress.ts`等）
- **型定義の置き場所**: 専用の`types/`フォルダは作らず、Supabaseの型を自動生成する運用も
  していない。型は使用箇所に一番近い`src/lib/<機能>/`内のファイルに`export type`で定義する
  （例: `AiSentence`型は`src/lib/quiz/ai.ts`に定義）
- **Supabaseクライアントの呼び出し方**: データアクセスは必ずServer Component/Server Action
  経由で行い、`@/lib/supabase/server`の`createClient()`を使う。クライアントコンポーネントから
  直接Supabaseを呼び出すことはしない（`src/lib/supabase/client.ts`は用意してあるが現状未使用）
- **認証チェック**: Server Actionの冒頭は必ず`const { data: { user } } = await supabase.auth.getUser();`
  → `if (!user) { redirect("/login"); }`という同一パターンで統一する
- **Server Actionの戻り値**: ユーザーに成功/失敗を伝える必要がある処理（AI呼び出し等）は
  `{ ok: true; ... } | { ok: false; error: string }`という判別可能なUnion型で返す。呼び出し側の
  画面は`error`をそのまま表示する設計にし、白画面やエラーで止まったままにしない
- **スタイリング**: 色・フォントサイズはReactのインラインstyleオブジェクトと、`globals.css`で
  定義したCSS変数（`var(--ink)`、`var(--card)`、`var(--grad)`等。5つのテーマ×ライト/ダーク
  モードぶん定義済み）を組み合わせて指定する。直書きの16進カラーコードや、Tailwindの固定色
  ユーティリティ（`bg-white`等）は使わない（テーマ切り替え・ダークモードの組み合わせで
  破綻するため）。2026-08-24のFigma Make全面リデザイン以降に手を入れる画面はこのインライン
  style方式に統一する（`/quiz/ai`等、リデザイン対象外だった画面にTailwindクラスが残っている
  箇所があるが、新規実装の基準にはしない）
- **`Math.random()`の扱い**: コンポーネント本体で直接呼び出さない（ESLintの
  `react-hooks/purity`に抵触する）。`src/lib/<機能>/`内のユーティリティ関数（例:
  `listening/pickRandom.ts`、`milestones/random.ts`）にラップしてから呼び出す
- **コメントの書き方**: 「何をしているか」の説明ではなく、「なぜそうしたか」（非自明な制約、
  過去に踏んだ不具合、仕様上の理由）だけを日本語で書く。自明なコードにコメントは付けない
- **マイグレーションファイル**: `supabase/migrations/YYYYMMDDHHMMSS_内容.sql`という命名で
  作成する。実行方法は「よく使うコマンド」を参照
- **コミットメッセージ**: 日本語で書く（英語は使わない）。1行目に変更内容を簡潔に、必要なら
  空行を挟んで本文に「なぜその変更をしたか（背景・指摘内容）」と「具体的に何を変えたか」を書く

## やってほしくないこと
- 既存の稼働中サイト（GitHub Pages上の各アプリ）のコードは
  勝手に上書き・削除しない。統合作業は必ずコピー先で行う
- package.json やビルド設定を確認なしに変更しない
- Supabaseの本番データベースに直接破壊的な変更を加えない
  （マイグレーションファイル経由で行う）

## よく使うコマンド
- `npm run dev` — 開発サーバーを起動する（http://localhost:3000）
- `npm run build` — 本番ビルド。デプロイ前の最終確認に使う
- `npx tsc --noEmit` — 型チェックのみ実行する（ビルドはしない）。実装後は必ず実行し、
  エラーが無いことを確認してから完了とする
- `npm run lint`（中身は`eslint`） — ESLintを実行する。実装後は必ず実行し、警告・エラーが
  無いことを確認してから完了とする
- **Supabaseのテーブル作成・変更**: Supabase CLIは導入していない。`supabase/migrations/`に
  SQLファイルを作成した上で、さおりさんにSupabaseダッシュボードのSQL Editorで手動実行して
  もらい、実行完了の確認を受けてから動作確認に進む（本番DBへ直接破壊的な変更を加えない
  ための運用。「やってほしくないこと」参照）
- **自動テスト**: Jest等のテストランナーは未導入で、`npm run test`に相当するコマンドは無い。
  動作確認は実際にログインしてブラウザ上で操作し、`tsc --noEmit`・ESLintと合わせて確認する

## 用語・ドメイン知識
- HSK: 中国政府公認の中国語能力試験（1級〜6級）
- 既存サイトの構成: 各アプリは単一ページのHTML/CSSで完結する
  シンプルな構成。統合時はこの学習コンテンツ自体を再利用する
- china_travel_phrasebook, mimic_chineseは今回のスコープ外。
  誤って統合対象に含めないよう注意する

---
※このファイルは開発が進むにつれて随時更新してください。
「未定」の項目は決まり次第、具体的な内容に書き換えます。
※docs/future-features.mdの内容が確定・実装フェーズに入ったら、
該当部分をこのCLAUDE.md本体に移してくる運用にしてください。
